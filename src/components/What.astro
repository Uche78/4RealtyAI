<!-- DEPENDENCIES -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

<style>
  /* 1. TYPOGRAPHY */
  .font-serif { font-family: 'Playfair Display', serif; }
  .font-sans { font-family: 'Inter', sans-serif; }

  /* 2. PRODUCT BENTO CARD */
  .product-card {
    background: #e7e5e0; /* Beige */
    padding: 2.5rem;
    border-radius: 1rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%;
    transition: all 0.5s cubic-bezier(0.2, 0.8, 0.2, 1);
    position: relative;
    overflow: hidden;
  }
  
  .product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px -10px rgba(44, 44, 44, 0.08);
    background: #e1dfda;
  }

  /* 3. MORTGAGE EXCLUSIVE CARD (Dark) */
  .product-card-dark {
    background: #1f1f1f; /* Charcoal */
    color: #F3F1EC;
    /* REMOVED: grid-column: 1 / -1; <--- This was forcing the new row */
  }
  .product-card-dark:hover {
    box-shadow: 0 20px 40px -10px rgba(44, 44, 44, 0.25);
    transform: translateY(-4px);
    background-color: #1c1c1c;
  }

  /* 4. PRODUCT TAG PILL */
  .product-tag {
    display: inline-block;
    font-family: 'Inter', sans-serif;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.25rem 0.75rem;
    border-radius: 99px;
    border: 1px solid rgba(44, 44, 44, 0.1);
    background: rgba(255, 255, 255, 0.4);
    margin-bottom: 1rem;
  }
  
  .product-card-dark .product-tag {
    border-color: rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.1);
    color: #fff;
  }

  /* 5. GRID UTILS */
  .products-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  @media (min-width: 768px) {
    .products-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (min-width: 1024px) {
    .products-grid { grid-template-columns: repeat(3, 1fr); }
  }
</style>

<section class="bg-[#F3F1EC] text-[#2c2c2c] py-24 px-6 overflow-hidden" id="products-section">
  
  <div class="max-w-7xl mx-auto">
    
    <!-- HEADER -->
    <div class="text-center mb-20 max-w-3xl mx-auto reveal-group">
      <div class="inline-block px-4 py-1.5 border border-[#2c2c2c]/10 rounded-full mb-6 bg- backdrop-blur-sm">
        <span class="text-[11px] font-sans font-medium text-[#2c2c2c]/60 uppercase tracking-[0.2em]">Our Ecosystem</span>
      </div>
      
      <h2 class="font-serif text-4xl md:text-5xl lg:text-6xl text-[#2c2c2c] mb-6 leading-[1.1]">
        Our AI <span class="italic text-[#2c2c2c]/70">Tool Suite</span>
      </h2>
      
      <p class="font-sans text-lg text-[#2c2c2c]/60 font-light leading-relaxed">
        We don't force you into everything at once. We identify the 1-2 tools that will drive the most revenue for you, implement those first, and expand from there.
      </p>
    </div>

    <!-- PRODUCTS GRID -->
    <div class="products-grid">
      
      <!-- 1. ShowSmart -->
      <div class="product-card group reveal-group">
        <div>
          <span class="product-tag">Chat Assistant</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">ShowSmart™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            Trained on your content. Answers questions instantly, captures leads, and books appointments 24/7.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"Your always-on qualification assistant"</p>
        </div>
      </div>

      <!-- 2. LeadBoost -->
      <div class="product-card group reveal-group" style="transition-delay: 50ms;">
        <div>
          <span class="product-tag">Content Gen</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">LeadBoost™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            Creates compelling listings, marketing copy, and email sequences in your exact brand voice.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"From photos to polished content—instantly"</p>
        </div>
      </div>

      <!-- 3. SocialSpark -->
      <div class="product-card group reveal-group" style="transition-delay: 100ms;">
        <div>
          <span class="product-tag">Social Auto</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">SocialSpark™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            Generates Instagram, Facebook, and LinkedIn posts automatically. Consistent engagement without the grind.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"Daily engagement, zero effort"</p>
        </div>
      </div>

      <!-- 4. AdGenie -->
      <div class="product-card group reveal-group" style="transition-delay: 150ms;">
        <div>
          <span class="product-tag">Ad Builder</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">AdGenie™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            Creates and manages Facebook & Google ads with smart targeting, A/B testing, and budget optimization.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"Your 24/7 ad strategist"</p>
        </div>
      </div>

      <!-- 5. MarketVision -->
      <div class="product-card group reveal-group" style="transition-delay: 200ms;">
        <div>
          <span class="product-tag">Analytics</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">MarketVision™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            AI-curated reports on market trends, pricing shifts, and demand patterns. Know exactly where to focus.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"Data-driven market domination"</p>
        </div>
      </div>

      <!-- 6. HomeMatch -->
      <div class="product-card group reveal-group" style="transition-delay: 250ms;">
        <div>
          <span class="product-tag">Recommender</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">HomeMatch™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            Learns client preferences and sends curated property or mortgage product alerts. Perfect matching every time.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"Your clients, perfectly matched"</p>
        </div>
      </div>
      
      <!-- 7. TourCast -->
      <!-- Only standard col-span-1 needed here -->
      <div class="product-card group reveal-group" style="transition-delay: 300ms;">
        <div>
          <span class="product-tag">Video AI</span>
          <h3 class="font-serif text-2xl text-[#2c2c2c] mb-4 group-hover:translate-x-1 transition-transform">TourCast™</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-6">
            Transforms listing photos into narrated video walkthroughs. Show more properties in less time.
          </p>
        </div>
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-sm italic text-[#2c2c2c]/80">"Show more. Sell faster."</p>
        </div>
      </div>

      <!-- 8. MORTGAGE EXCLUSIVE -->
      <!-- 
           ON DESKTOP (lg): spans 2 columns (2 & 3), sitting next to TourCast.
           ON TABLET (md): spans 1 column, sitting next to TourCast.
           ON MOBILE: stacks normally.
      -->
      <div class="product-card product-card-dark lg:col-span-2 group reveal-group" style="transition-delay: 350ms;">
        <!-- Texture -->
        <div class="absolute inset-0 opacity-10 bg-[radial-gradient(#fff_1px,transparent_1px)] [background-size:16px_16px]"></div>
        
        <div class="relative z-10 grid md:grid-cols-2 gap-8 items-center h-full">
          <div>
            <div class="flex items-center gap-3 mb-4">
              <span class="product-tag">Mortgage Exclusive</span>
            </div>
            <h3 class="font-serif text-3xl md:text-4xl text-white mb-4">DealCheck™</h3>
            <p class="font-sans text-lg text-white/80 mb-6 font-light">
              Canadian mortgage analysis with risk scoring, fraud detection, and CMHC compliance checks.
            </p>
            <p class="font-serif text-xl italic text-white/60">"Catch problems before your lender does"</p>
          </div>
          
          <div class="bg-white/5 rounded-xl p-6 border border-white/10 h-full flex items-center">
            <ul class="space-y-3 font-sans text-sm text-white/70">
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 text-white flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
                Analyzes income, assets & liabilities
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 text-white flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
                Canadian-specific (Stress test, Provincial)
              </li>
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 text-white flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
                Document fraud detection
              </li>
            </ul>
          </div>
        </div>
      </div>
      
    </div>
  </div>
</section>

<!-- SCROLL REVEAL SCRIPT -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-100', 'translate-y-0');
          entry.target.classList.remove('opacity-0', 'translate-y-10');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    const items = document.querySelectorAll('.reveal-group');
    items.forEach((item) => {
      item.classList.add('transition-all', 'duration-1000', 'ease-out', 'opacity-0', 'translate-y-10');
      observer.observe(item);
    });
  });
</script>