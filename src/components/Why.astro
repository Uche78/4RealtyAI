<!-- DEPENDENCIES -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

<style>
  /* 1. TYPOGRAPHY */
  .font-serif { font-family: 'Playfair Display', serif; }
  .font-sans { font-family: 'Inter', sans-serif; }

  /* 2. IMAGE STYLING */
  .luxury-image-container {
    position: relative;
    overflow: hidden;
    border-radius: 12px;
  }
  
  .luxury-image {
    transition: transform 1.5s cubic-bezier(0.2, 0.8, 0.2, 1);
  }
  .luxury-image-container:hover .luxury-image {
    transform: scale(1.05);
  }

  /* 3. FLOATING GLASS BADGE */
  .glass-badge {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.4);
    box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.1);
  }

  /* 4. EDITORIAL LIST */
  .editorial-list-item {
    border-bottom: 1px solid rgba(44, 44, 44, 0.1);
    padding-bottom: 1rem;
    margin-bottom: 1rem;
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    transition: all 0.3s ease;
  }
  .editorial-list-item:last-child {
    border-bottom: none;
  }
  .editorial-list-item:hover {
    padding-left: 0.5rem;
    border-color: rgba(44, 44, 44, 0.3);
  }
</style>

<section id="why" class="bg-[#F3F1EC] text-[#2c2c2c] py-24 px-6 overflow-hidden">
  <div class="max-w-7xl mx-auto w-full">
    
    <!-- HEADER -->
    <div class="mb-20 max-w-4xl reveal-group">
      <div class="inline-block px-4 py-1.5 border border-[#2c2c2c]/10 rounded-full mb-6 bg- backdrop-blur-sm">
        <span class="text-[11px] font-sans font-medium text-[#2c2c2c]/60 uppercase tracking-[0.2em]">Why 4RealtyAI</span>
      </div>
      
      <h2 class="font-serif text-4xl md:text-5xl lg:text-6xl text-[#2c2c2c] mb-6 leading-[1.1]">
        Built by Industry Experts. <br/>
        <span class="italic text-[#2c2c2c]/70">Designed for Results.</span>
      </h2> 

      <p class="font-sans text-lg text-[#2c2c2c]/70 max-w-3xl leading-relaxed font-light"> 
        We combine AI consulting, custom implementation, and our own tools to deliver real-world results. Our systems are informed by industry studies and field-tested with agents and brokers across Toronto and the GTA.
      </p>
    </div>
    
    <!-- SPLIT LAYOUT -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      
      <!-- LEFT COLUMN: Content -->
      <div class="order-2 lg:order-1 reveal-group" style="transition-delay: 100ms;">
        <h3 class="font-serif text-3xl text-[#2c2c2c] mb-8 leading-tight">
          Systems that work <br/><span class="italic">while you sleep.</span>
        </h3>

        <p class="font-sans text-sm md:text-base text-[#2c2c2c]/70 leading-relaxed mb-10">
          We help you automate lead capture, marketing, and client engagement with systems that work 24/7. No missed leads. No more tool chaos. Just seamless, smart automation.
        </p>

        <!-- Editorial Checklist -->
        <div class="space-y-2">
          <div class="editorial-list-item group">
             <span class="font-sans text-xs font-bold uppercase tracking-widest text-[#2c2c2c]/40 mt-1 w-8">01</span>
             <div>
               <p class="font-sans font-medium text-[#2c2c2c]">40–60% less manual work</p>
               <p class="font-sans text-xs text-[#2c2c2c]/50 mt-1">Free up time for high-value client interactions.</p>
             </div>
          </div>
          <div class="editorial-list-item group">
             <span class="font-sans text-xs font-bold uppercase tracking-widest text-[#2c2c2c]/40 mt-1 w-8">02</span>
             <div>
               <p class="font-sans font-medium text-[#2c2c2c]">Consistent response times</p>
               <p class="font-sans text-xs text-[#2c2c2c]/50 mt-1">Instant engagement, 24/7/365.</p>
             </div>
          </div>
          <div class="editorial-list-item group">
             <span class="font-sans text-xs font-bold uppercase tracking-widest text-[#2c2c2c]/40 mt-1 w-8">03</span>
             <div>
               <p class="font-sans font-medium text-[#2c2c2c]">Reduced tech bloat</p>
               <p class="font-sans text-xs text-[#2c2c2c]/50 mt-1">Consolidate overlapping tools and save costs.</p>
             </div>
          </div>
          <div class="editorial-list-item group">
             <span class="font-sans text-xs font-bold uppercase tracking-widest text-[#2c2c2c]/40 mt-1 w-8">04</span>
             <div>
               <p class="font-sans font-medium text-[#2c2c2c]">Data-backed decisions</p>
               <p class="font-sans text-xs text-[#2c2c2c]/50 mt-1">Stop guessing. Start scaling with clarity.</p>
             </div>
          </div>
        </div>
      </div>
      
      <!-- RIGHT COLUMN: Image with Floating Card -->
      <div class="order-1 lg:order-2 reveal-group">
        <div class="relative pl-0 lg:pl-10">
          
          <!-- Main Image -->
          <div class="luxury-image-container shadow-2xl">
            <!-- Overlay Gradient for text readability if needed -->
            <div class="absolute inset-0 bg-[#2c2c2c]/10 z-10 pointer-events-none"></div>
            
            <img 
              src="toronto.jpg" 
              alt="Real estate agent in Toronto" 
              class="luxury-image w-full h-[500px] object-cover"
            >
          </div>

          <!-- Floating Glass Card (Bento Style) -->
          <div class="absolute -bottom-8 -left-4 lg:-left-8 glass-badge p-6 rounded-xl max-w-xs z-20">
            <div class="flex items-center gap-4 mb-3">
              <div class="w-10 h-10 bg-[#2c2c2c] rounded-full flex items-center justify-center text-white">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/></svg>
              </div>
              <div>
                <p class="font-sans text-xs font-bold uppercase tracking-wider text-[#2c2c2c]/50">Field Tested</p>
                <p class="font-serif text-lg text-[#2c2c2c]">Proven Results</p>
              </div>
            </div>
            <p class="font-sans text-xs text-[#2c2c2c]/70 leading-relaxed">
              Strategies validated by top-performing agents across the GTA market.
            </p>
          </div>

        </div>        
      </div>

    </div>
  </div>
</section>

<!-- SCROLL REVEAL SCRIPT -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-100', 'translate-y-0');
          entry.target.classList.remove('opacity-0', 'translate-y-10');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    const items = document.querySelectorAll('.reveal-group');
    items.forEach((item) => {
      item.classList.add('transition-all', 'duration-1000', 'ease-out', 'opacity-0', 'translate-y-10');
      observer.observe(item);
    });
  });
</script>