<!-- DEPENDENCIES -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

<style>
  /* 1. TYPOGRAPHY */
  .font-serif { font-family: 'Playfair Display', serif; }
  .font-sans { font-family: 'Inter', sans-serif; }

  /* 2. LAYOUT */
  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  @media (min-width: 1024px) {
    .pricing-grid {
      grid-template-columns: 1fr 1fr;
    }
    .col-span-full {
      grid-column: 1 / -1;
    }
  }

  /* 3. LUXURY CARDS */
  .price-card {
    background: #e7e5e0; /* Beige */
    padding: 3rem;
    border-radius: 1rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    transition: all 0.5s cubic-bezier(0.2, 0.8, 0.2, 1);
    height: 100%;
    position: relative;
    overflow: hidden;
  }
  
  .price-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px -10px rgba(44, 44, 44, 0.08);
  }

  /* Dark Card (The "Partner" option) */
  .price-card-dark {
    background: #1f1f1f; /* Charcoal */
    color: #F3F1EC;
  }
  .price-card-dark:hover {
    box-shadow: 0 20px 40px -10px rgba(44, 44, 44, 0.25);
    background-color: #1c1c1c;
  }

  /* 4. LIST STYLING */
  .feature-list li {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    margin-bottom: 1rem;
    font-family: 'Inter', sans-serif;
    font-size: 0.95rem;
    line-height: 1.5;
  }
</style>

<section class="bg-[#F3F1EC] text-[#2c2c2c] py-24 px-6 overflow-hidden" id="pricing">
  
  <div class="max-w-6xl mx-auto">
    
    <!-- HEADER -->
    <div class="text-center mb-20 max-w-3xl mx-auto reveal-group">
      <div class="inline-block px-4 py-1.5 border border-[#2c2c2c]/10 rounded-full mb-6 bg- backdrop-blur-sm">
        <span class="text-[11px] font-sans font-medium text-[#2c2c2c]/60 uppercase tracking-[0.2em]">Transparent Pricing</span>
      </div>
      
      <h2 class="font-serif text-4xl md:text-5xl lg:text-6xl text-[#2c2c2c] mb-6 leading-[1.1]">
        We only win <br/><span class="italic text-[#2c2c2c]/70">when you win.</span>
      </h2>
      
      <p class="font-sans text-lg text-[#2c2c2c]/60 font-light leading-relaxed">
        Our model aligns with your success. You pay for implementation upfront, then we share in the revenue our systems generate.
      </p>
    </div>

    <!-- PRICING GRID -->
    <div class="pricing-grid">
      
      <!-- CARD 1: SETUP (Beige) -->
      <div class="price-card group reveal-group">
        <div>
          <h3 class="font-serif text-3xl text-[#2c2c2c] mb-2">Implementation</h3>
          <p class="font-sans text-sm text-[#2c2c2c]/60 uppercase tracking-wider mb-8">One-Time Setup Fee</p>
          
          <ul class="feature-list text-[#2c2c2c]/80 mb-8">
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              Workflow analysis & opportunity audit
            </li>
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              Tool customization (Brand & Voice)
            </li>
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              CRM integration & Data hygiene
            </li>
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              Team training & Documentation
            </li>
          </ul>
        </div>
        
        <div class="pt-8 border-t border-[#2c2c2c]/10">
          <p class="font-serif text-4xl md:text-5xl text-[#2c2c2c] mb-2">$3.5k<span class="text-2xl opacity-50">–</span>15k</p>
          <p class="font-sans text-xs text-[#2c2c2c]/50">Investment varies based on complexity</p>
        </div>
      </div>

      <!-- CARD 2: PERFORMANCE (Charcoal) -->
      <div class="price-card price-card-dark group reveal-group" style="transition-delay: 150ms;">
        <!-- Texture -->
        <div class="absolute inset-0 opacity-10 bg-[radial-gradient(#fff_1px,transparent_1px)] [background-size:16px_16px]"></div>
        
        <div class="relative z-10">
          <div class="flex justify-between items-start mb-2">
            <h3 class="font-serif text-3xl text-white">Partnership</h3>
            <span class="px-3 py-1 bg-white/10 rounded-full text-[10px] font-sans font-bold uppercase tracking-widest text-white border border-white/20">
              Results Based
            </span>
          </div>
          <p class="font-sans text-sm text-white/50 uppercase tracking-wider mb-8">Monthly Performance Fee</p>
          
          <ul class="feature-list text-white/80 mb-8">
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              Revenue Share (1.5% – 3.5%)
            </li>
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              Ongoing optimization included
            </li>
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              Attribution reporting
            </li>
            <li>
              <svg class="w-5 h-5 flex-shrink-0 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
              No long-term contracts
            </li>
          </ul>
        </div>
        
        <div class="relative z-10 pt-8 border-t border-white/10">
          <p class="font-serif text-3xl md:text-4xl text-white mb-2 leading-tight">
            No Revenue? <br/><span class="italic text-white/50">We don't get paid.</span>
          </p>
        </div>
      </div>
      
      <!-- CARD 3: CTA (Full Width) -->
      <div class="price-card col-span-full group reveal-group" style="background: #; border: 1px solid #e7e5e0; transition-delay: 300ms;">
        <div class="flex flex-col md:flex-row items-center justify-between gap-8 text-center md:text-left">
          <div class="max-w-2xl">
            <h3 class="font-serif text-2xl text-[#2c2c2c] mb-2">Every business is unique.</h3>
            <p class="font-sans text-sm text-[#2c2c2c]/60">
              Book a free assessment. We will show you exactly what implementation will cost and the revenue growth you can expect.
            </p>
          </div>
          
          <div class="flex-shrink-0">
             <a href="https://assessment.4realtyai.com/assessment" class="group relative inline-flex items-center gap-3 px-8 py-4 bg-[#2c2c2c] text-white rounded-full overflow-hidden transition-all hover:pr-12">
               <span class="font-sans font-medium relative z-10">Get Your Free Assessment</span>
               <div class="absolute right-4 translate-x-10 group-hover:translate-x-0 transition-transform duration-300">
                 <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
               </div>
             </a>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- SCROLL REVEAL SCRIPT -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-100', 'translate-y-0');
          entry.target.classList.remove('opacity-0', 'translate-y-10');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    const items = document.querySelectorAll('.reveal-group');
    items.forEach((item) => {
      item.classList.add('transition-all', 'duration-1000', 'ease-out', 'opacity-0', 'translate-y-10');
      observer.observe(item);
    });
  });
</script>