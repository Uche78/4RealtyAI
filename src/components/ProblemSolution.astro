<!-- 1. FONTS & ICONS -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

<style>
  /* 2. TYPOGRAPHY */
  .font-serif { font-family: 'Playfair Display', serif; }
  .font-sans { font-family: 'Inter', sans-serif; }

  /* 3. LUXURY BENTO CARD (Matches Partnership Section) */
  .luxury-card {
    position: relative;
    background: #e7e5e0; /* Darker beige card */
    padding: 2.5rem;
    border-radius: 1rem;
    transition: all 0.5s cubic-bezier(0.2, 0.8, 0.2, 1);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%;
    overflow: hidden;
  }

  .luxury-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px -10px rgba(44, 44, 44, 0.08);
    background: #e1dfda; /* Subtle darken on hover */
  }

  /* Dark Card Variant (For the Bottom Insight) */
  .luxury-card-dark {
    background: #1f1f1f;
    color: #F3F1EC;
  }
  .luxury-card-dark:hover {
    box-shadow: 0 20px 40px -10px rgba(44, 44, 44, 0.2);
    transform: translateY(-4px);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    background-color: #1c1c1c;
  }

  /* 4. HOVER ARROW ANIMATION */
  .card-icon-arrow {
    opacity: 0;
    transform: translate(-10px, 10px);
    transition: all 0.4s ease;
    color: #2c2c2c;
  }
  .luxury-card:hover .card-icon-arrow {
    opacity: 0.3;
    transform: translate(0, 0);
  }

  /* 5. GRID UTILS */
  .problem-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  
  @media (min-width: 1024px) {
    .problem-grid {
      grid-template-columns: repeat(3, 1fr);
    }
    .col-span-full {
      grid-column: 1 / -1;
    }
  }
</style>

<section class="bg-[#F3F1EC] text-[#2c2c2c] py-24 px-6 overflow-hidden" id="problem-section">
  
  <div class="max-w-7xl mx-auto">
    
    <!-- HEADER -->
    <div class="text-center mb-20 max-w-4xl mx-auto reveal-group">
      <!-- Badge -->
      <div class="inline-block px-4 py-1.5 border border-[#2c2c2c]/10 rounded-full mb-6 bg- backdrop-blur-sm">
        <span class="text-[11px] font-sans font-medium text-[#2c2c2c]/60 uppercase tracking-[0.2em]">Market Insight</span>
      </div>
      
      <!-- Headline -->
      <h2 class="font-serif text-4xl md:text-5xl lg:text-6xl text-[#2c2c2c] mb-6 leading-[1.1]">
        Why <span class="italic text-[#2c2c2c]/70">88% of firms</span> fail at AI
      </h2>
      
      <!-- Subheadline -->
      <p class="font-sans text-lg text-[#2c2c2c]/60 max-w-2xl mx-auto font-light leading-relaxed">
        JLL's research identified three critical gaps. Most get stuck in the "Tech Trap." Here is how to join the 5% that actually generate revenue.
      </p>
    </div>

    <!-- BENTO GRID -->
    <div class="problem-grid">
      
      <!-- CARD 1: Infrastructure -->
      <div class="luxury-card group reveal-group">
        <div>
           <!-- Hover Arrow -->
           <div class="absolute top-8 right-8 card-icon-arrow">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H7M17 7V17"/></svg>
           </div>
           
           <!-- Number Circle -->
           <div class="w-10 h-10 border border-[#2c2c2c]/10 rounded-full flex items-center justify-center mb-6 bg-white/30">
             <span class="font-sans text-xs font-bold text-[#2c2c2c]">01</span>
           </div>
           
           <h3 class="font-serif text-2xl md:text-3xl text-[#2c2c2c] mb-4 leading-tight group-hover:translate-x-1 transition-transform">
             Broken <br/>Infrastructure
           </h3>
           
           <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-8">
             Most firms build AI on top of disconnected CRMs and duplicate data. AI needs clean inputs to work properly.
           </p>
        </div>

        <!-- The Fix Section (Bottom of card) -->
        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-sans text-[10px] font-bold text-[#2c2c2c]/40 uppercase tracking-widest mb-2">The Solution</p>
          <p class="font-sans text-sm text-[#2c2c2c]/90 font-medium">
            We audit setup and prevent future data mess. We build the foundation first.
          </p>
        </div>
      </div>

      <!-- CARD 2: Tools -->
      <div class="luxury-card group reveal-group" style="transition-delay: 100ms;">
        <div>
           <div class="absolute top-8 right-8 card-icon-arrow">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H7M17 7V17"/></svg>
           </div>
           
           <div class="w-10 h-10 border border-[#2c2c2c]/10 rounded-full flex items-center justify-center mb-6 bg-white/30">
             <span class="font-sans text-xs font-bold text-[#2c2c2c]">02</span>
           </div>
           
           <h3 class="font-serif text-2xl md:text-3xl text-[#2c2c2c] mb-4 leading-tight group-hover:translate-x-1 transition-transform">
             Disconnected <br/><span class="italic">Tools</span>
           </h3>
           
           <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-8">
             Your chatbot doesn't sync with your CRM. You're manually copying info. That's not automation—that's chores.
           </p>
        </div>

        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-sans text-[10px] font-bold text-[#2c2c2c]/40 uppercase tracking-widest mb-2">The Solution</p>
          <p class="font-sans text-sm text-[#2c2c2c]/90 font-medium">
            We integrate your systems so data flows automatically. No manual copying.
          </p>
        </div>
      </div>

      <!-- CARD 3: Adoption -->
      <div class="luxury-card group reveal-group" style="transition-delay: 200ms;">
        <div>
           <div class="absolute top-8 right-8 card-icon-arrow">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H7M17 7V17"/></svg>
           </div>
           
           <div class="w-10 h-10 border border-[#2c2c2c]/10 rounded-full flex items-center justify-center mb-6 bg-white/30">
             <span class="font-sans text-xs font-bold text-[#2c2c2c]">03</span>
           </div>
           
           <h3 class="font-serif text-2xl md:text-3xl text-[#2c2c2c] mb-4 leading-tight group-hover:translate-x-1 transition-transform">
             Zero <br/>Adoption
           </h3>
           
           <p class="font-sans text-sm text-[#2c2c2c]/70 leading-relaxed mb-8">
             You bought it. You tried it. It's collecting dust. Without training and accountability, everyone quits.
           </p>
        </div>

        <div class="pt-6 border-t border-[#2c2c2c]/10">
          <p class="font-sans text-[10px] font-bold text-[#2c2c2c]/40 uppercase tracking-widest mb-2">The Solution</p>
          <p class="font-sans text-sm text-[#2c2c2c]/90 font-medium">
            We train your team, build adoption into workflows, and monitor usage.
          </p>
        </div>
      </div>

      <!-- CARD 4: Bottom Insight (Wide Bento Tile) -->
      <div class="luxury-card luxury-card-dark col-span-full group reveal-group" style="transition-delay: 300ms;">
        <!-- Texture Pattern -->
        <div class="bg-noise"></div>
        <div class="absolute inset-0 opacity-10 bg-[radial-gradient(#fff_1px,transparent_1px)] [background-size:16px_16px]"></div>
        
        <div class="relative z-10 flex flex-col md:flex-row items-center justify-between gap-6 text-center md:text-left">
          <div class="flex-1">
             <p class="font-sans text-sm md:text-base text-white/50 tracking-wide mb-2 uppercase">The Takeaway</p>
             <p class="font-serif text-2xl md:text-4xl text-white leading-tight">
               The revenue gap isn't magic. <span class="italic text-white/60">It's implementation.</span>
             </p>
          </div>
          
          <div class="flex-shrink-0">
            <a href="https://calendly.com/4realtyai/30min" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 px-8 py-3 bg-[#F3F1EC] text-[#2c2c2c] rounded-full font-sans font-medium text-sm hover:bg-[#e7e5e0] transition-colors">
              Fix Your Implementation
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
            </a>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- SCROLL REVEAL SCRIPT -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-100', 'translate-y-0');
          entry.target.classList.remove('opacity-0', 'translate-y-10');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    const items = document.querySelectorAll('.reveal-group');
    items.forEach((item) => {
      item.classList.add('transition-all', 'duration-1000', 'ease-out', 'opacity-0', 'translate-y-10');
      observer.observe(item);
    });
  });
</script>