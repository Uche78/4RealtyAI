<!-- DEPENDENCIES -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

<style>
  /* 1. TYPOGRAPHY */
  .font-serif { font-family: 'Playfair Display', serif; }
  .font-sans { font-family: 'Inter', sans-serif; }

  /* 2. ACCORDION ANIMATION */
  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.5s cubic-bezier(0.2, 0.8, 0.2, 1);
  }
  
  .faq-icon {
    transition: transform 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
  }
  
  /* Rotate icon when active */
  .faq-btn[aria-expanded="true"] .faq-icon {
    transform: rotate(180deg);
  }
  
  /* Subtle highlight on hover */
  .faq-item:hover .faq-question-text {
    color: #000000; /* Darken text slightly */
  }
</style>

<section id="faq" class="py-24 px-6 bg-[#F3F1EC] text-[#2c2c2c]">
  <div class="max-w-4xl mx-auto">
    
    <!-- HEADER -->
    <div class="text-center mb-16 reveal-group">
      <div class="inline-block px-4 py-1.5 border border-[#2c2c2c]/10 rounded-full mb-6 bg- backdrop-blur-sm">
        <span class="text-[11px] font-sans font-medium text-[#2c2c2c]/60 uppercase tracking-[0.2em]">Common Questions</span>
      </div>
      
      <h2 class="font-serif text-4xl md:text-5xl lg:text-6xl text-[#2c2c2c] mb-6 leading-[1.1]">
        Frequently Asked <br/><span class="italic text-[#2c2c2c]/70">Questions</span>
      </h2>
      
      <p class="font-sans text-lg text-[#2c2c2c]/60 max-w-2xl mx-auto font-light leading-relaxed">
        Get answers to common questions about implementing AI in your real estate business.
      </p>
    </div>

    <!-- FAQ LIST (Editorial Style) -->
    <div class="border-t border-[#2c2c2c]/10">
      
      <!-- ITEM 1 -->
      <div class="faq-item border-b border-[#2c2c2c]/10 reveal-group">
        <button class="faq-btn w-full py-8 flex justify-between items-start text-left group focus:outline-none" aria-expanded="false">
          <span class="faq-question-text font-serif text-xl md:text-2xl text-[#2c2c2c] pr-8 transition-colors duration-300">
            I'm not tech-savvy. Will this be too complicated?
          </span>
          <span class="flex-shrink-0 mt-1 p-2 rounded-full border border-[#2c2c2c]/10 group-hover:bg-white group-hover:shadow-sm transition-all duration-300">
            <svg class="faq-icon w-4 h-4 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </span>
        </button>
        <div class="faq-answer">
          <div class="pb-8 font-sans text-base text-[#2c2c2c]/70 leading-relaxed font-light max-w-2xl">
            That's exactly why we focus on implementation, not just selling software. We set everything up, train your team, and provide ongoing support. If you can use a smartphone, you can use our systems.
          </div>
        </div>
      </div>

      <!-- ITEM 2 -->
      <div class="faq-item border-b border-[#2c2c2c]/10 reveal-group" style="transition-delay: 50ms;">
        <button class="faq-btn w-full py-8 flex justify-between items-start text-left group focus:outline-none" aria-expanded="false">
          <span class="faq-question-text font-serif text-xl md:text-2xl text-[#2c2c2c] pr-8 transition-colors duration-300">
            I already have a CRM. Will this work with it?
          </span>
          <span class="flex-shrink-0 mt-1 p-2 rounded-full border border-[#2c2c2c]/10 group-hover:bg-white group-hover:shadow-sm transition-all duration-300">
            <svg class="faq-icon w-4 h-4 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
          </span>
        </button>
        <div class="faq-answer">
          <div class="pb-8 font-sans text-base text-[#2c2c2c]/70 leading-relaxed font-light max-w-2xl">
            Yes. We integrate with all major real estate CRMs including Follow Up Boss, Salesforce, HubSpot, Real Geeks, LionDesk, and more. We sync data automatically so you don't have duplicate tools.
          </div>
        </div>
      </div>

      <!-- ITEM 3 -->
      <div class="faq-item border-b border-[#2c2c2c]/10 reveal-group" style="transition-delay: 100ms;">
        <button class="faq-btn w-full py-8 flex justify-between items-start text-left group focus:outline-none" aria-expanded="false">
          <span class="faq-question-text font-serif text-xl md:text-2xl text-[#2c2c2c] pr-8 transition-colors duration-300">
            How long until I see results?
          </span>
          <span class="flex-shrink-0 mt-1 p-2 rounded-full border border-[#2c2c2c]/10 group-hover:bg-white group-hover:shadow-sm transition-all duration-300">
            <svg class="faq-icon w-4 h-4 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
          </span>
        </button>
        <div class="faq-answer">
          <div class="pb-8 font-sans text-base text-[#2c2c2c]/70 leading-relaxed font-light max-w-2xl">
            Most agents see captured leads within the first week of going live. Full automation benefits (time savings, better marketing) typically materialize within 30-45 days as systems optimize.
          </div>
        </div>
      </div>

      <!-- ITEM 4 -->
      <div class="faq-item border-b border-[#2c2c2c]/10 reveal-group" style="transition-delay: 150ms;">
        <button class="faq-btn w-full py-8 flex justify-between items-start text-left group focus:outline-none" aria-expanded="false">
          <span class="faq-question-text font-serif text-xl md:text-2xl text-[#2c2c2c] pr-8 transition-colors duration-300">
            What if the AI gives wrong information?
          </span>
          <span class="flex-shrink-0 mt-1 p-2 rounded-full border border-[#2c2c2c]/10 group-hover:bg-white group-hover:shadow-sm transition-all duration-300">
            <svg class="faq-icon w-4 h-4 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
          </span>
        </button>
        <div class="faq-answer">
          <div class="pb-8 font-sans text-base text-[#2c2c2c]/70 leading-relaxed font-light max-w-2xl">
            ShowSmart™ only uses information from YOUR website and listings. You control what it knows. Plus, it's programmed to transfer complex questions to you rather than guessing. You can review all conversations.
          </div>
        </div>
      </div>

      <!-- ITEM 5 -->
      <div class="faq-item border-b border-[#2c2c2c]/10 reveal-group" style="transition-delay: 200ms;">
        <button class="faq-btn w-full py-8 flex justify-between items-start text-left group focus:outline-none" aria-expanded="false">
          <span class="faq-question-text font-serif text-xl md:text-2xl text-[#2c2c2c] pr-8 transition-colors duration-300">
            Can I cancel anytime?
          </span>
          <span class="flex-shrink-0 mt-1 p-2 rounded-full border border-[#2c2c2c]/10 group-hover:bg-white group-hover:shadow-sm transition-all duration-300">
            <svg class="faq-icon w-4 h-4 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
          </span>
        </button>
        <div class="faq-answer">
          <div class="pb-8 font-sans text-base text-[#2c2c2c]/70 leading-relaxed font-light max-w-2xl">
            Yes. All our tool subscriptions are month-to-month. Implementation fees are separate and non-refundable after 30 days (unless you qualify for our ROI guarantee).
          </div>
        </div>
      </div>

      <!-- ITEM 6 -->
      <div class="faq-item border-b border-[#2c2c2c]/10 reveal-group" style="transition-delay: 250ms;">
        <button class="faq-btn w-full py-8 flex justify-between items-start text-left group focus:outline-none" aria-expanded="false">
          <span class="faq-question-text font-serif text-xl md:text-2xl text-[#2c2c2c] pr-8 transition-colors duration-300">
            Do I need to be in Toronto?
          </span>
          <span class="flex-shrink-0 mt-1 p-2 rounded-full border border-[#2c2c2c]/10 group-hover:bg-white group-hover:shadow-sm transition-all duration-300">
            <svg class="faq-icon w-4 h-4 text-[#2c2c2c]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
          </span>
        </button>
        <div class="faq-answer">
          <div class="pb-8 font-sans text-base text-[#2c2c2c]/70 leading-relaxed font-light max-w-2xl">
            No. While we're based in Toronto and have done extensive testing in the GTA, our systems work for agents and brokers anywhere in North America. All implementation and training can be done remotely.
          </div>
        </div>
      </div>

    </div>

    <!-- BOTTOM CTA -->
    <div class="mt-20 text-center reveal-group">
      <p class="font-serif text-xl text-[#2c2c2c] mb-6">
        Still have questions?
      </p>
      <div class="inline-flex items-center gap-2 px-6 py-3 bg-[#e7e5e0] rounded-full text-[#2c2c2c] hover:bg-white border border-[#2c2c2c]/5 transition-all cursor-pointer">
        <span class="w-2 h-2 rounded-full bg-green-500"></span>
        <span class="font-sans text-sm font-medium">Chat with our AI Agent (Bottom Right)</span>
      </div>
    </div>
  </div>
</section>

<!-- SCRIPTS: Accordion Logic + Scroll Reveal -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    
    // 1. Accordion Logic
    const faqButtons = document.querySelectorAll('.faq-btn');
    
    faqButtons.forEach(btn => {
      btn.addEventListener('click', () => {
        const answer = btn.nextElementSibling as HTMLElement | null;
        if (!answer) return;
        const isOpen = btn.getAttribute('aria-expanded') === 'true';
        
        // Close all others
        faqButtons.forEach(otherBtn => {
          if (otherBtn !== btn) {
            otherBtn.setAttribute('aria-expanded', 'false');
            otherBtn.nextElementSibling.style.maxHeight = '0px';
          }
        });
        
        // Toggle current
        if (isOpen) {
          btn.setAttribute('aria-expanded', 'false');
          answer.style.maxHeight = '0px';
        } else {
          btn.setAttribute('aria-expanded', 'true');
          answer.style.maxHeight = answer.scrollHeight + 'px';
        }
      });
    });

    // 2. Scroll Reveal
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-100', 'translate-y-0');
          entry.target.classList.remove('opacity-0', 'translate-y-10');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    const items = document.querySelectorAll('.reveal-group');
    items.forEach((item) => {
      item.classList.add('transition-all', 'duration-1000', 'ease-out', 'opacity-0', 'translate-y-10');
      observer.observe(item);
    });
  });
</script>
